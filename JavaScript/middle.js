/*
    난이도 : 중
*/
(function () {
    const object = { flag: 'dimigo', kdmhs: 'just', admission: 'true' }
    const temporary = object['flag']
    switch (temporary) {
        case 'dlmigo': return 'a819ml'
        case 'just': return '03m1la'
        case 'admission': return '9ml3an'
        default: return 'mvn30a'
    }
})()